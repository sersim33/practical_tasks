numbers = [10, 5, 11, 2, 3, 5, 8, 9, 3, 4, 2]

# Перетворюємо список у множину, щоб отримати тільки унікальні значення,
# а потім перетворюємо множину у список і сортуємо його
unique_numbers = sorted(list(set(numbers)))

# Виводимо унікальні значення у відсортованому порядку
print(unique_numbers)